# Bundled FFmpeg binaries

Place LGPL-compatible Windows x64 builds here before packaging:

- `ffmpeg.exe`
- `ffprobe.exe`

Also place the corresponding FFmpeg license files and source offer/URL text in this folder or in the repository root notices before distributing builds.
